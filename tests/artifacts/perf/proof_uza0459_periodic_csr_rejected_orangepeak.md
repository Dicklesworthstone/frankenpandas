# br-frankenpandas-uza04.59 proof - periodic-key CSR certificate rejected

## Baseline and profile

- Head: `129b2f34`.
- Build: `CARGO_TARGET_DIR=/data/projects/.scratch/cargo-target-orangepeak-uza0459-base RUSTFLAGS='-C force-frame-pointers=yes' rch exec -- cargo build -p fp-conformance --profile release-perf --example perf_profile`.
- Golden baseline SHA256 for `perf_profile golden outer_join 20000`: `453b318a10d828fcf65cef61898a965ea4e1a402e041cb45b0a7a4001b65b750`.
- Hyperfine baseline for `outer_join 500000 20`: `319.5 ms +/- 10.8 ms`.
- Fresh profile for `outer_join 500000 200`: `10.669 ms/iter`, `build_single_key_dense_i64_outer_merge_output` `78.18%` children, promoted left f64 materialization closure `20.43%`, generic CSR construction closure `16.31%`, promoted right f64 tape closure `15.19%`, and plan push around `8.28%`.

## Lever rejected

One lever attempted in `crates/fp-join/src/lib.rs`: a private periodic dense-i64 CSR certificate helper for the fused dense outer route. It scanned the real key slice, proved a consecutive modulo cycle, derived bucket offsets and row positions from the certificate, and fell back to the existing generic CSR builder on gaps, shifted-span overflow, mismatched cycles, or non-periodic input.

The source hunk was removed after the score gate rejected it. No `.59` source change remains.

## Isomorphism proof while candidate was present

- Output ordering: unchanged. Certified positions were emitted bucket-major, with per-bucket positions increasing by the proved period, matching the generic CSR positions generated by the existing row-order cursor fill.
- Tie-breaking: unchanged. Matched buckets still consumed left positions in input order and right bucket positions in input order.
- Floating point: unchanged. The lever only changed CSR offsets/positions; promoted values still used the existing `i64 as f64` materialization paths.
- Null/NaN semantics: unchanged. Missing-side detection, sparse validity witnesses, and nullable constructors were untouched.
- RNG/hash behavior: unchanged. No RNG or hash-table iteration path changed.
- Golden output: after SHA256 stayed `453b318a10d828fcf65cef61898a965ea4e1a402e041cb45b0a7a4001b65b750`; `cmp=0`.

## Benchmarks

Paired `outer_join 500000 20`:

- Baseline: `309.6 ms +/- 11.0 ms`.
- Candidate: `321.6 ms +/- 17.6 ms`.
- Ratio: baseline ran `1.04x +/- 0.07` faster.

Confirmation `outer_join 500000 200`:

- Baseline: `2.083 s +/- 0.087 s`.
- Candidate: `2.311 s +/- 0.048 s`.
- Ratio: baseline ran `1.11x +/- 0.05` faster.

Score: Impact `0` x Confidence `4` / Effort `2` = `0.0`; reject.

## Validation

- `cargo fmt -p fp-join`: passed while the candidate was present.
- `rch exec -- cargo test -p fp-join periodic_dense_i64_csr --lib`: passed in isolated target after avoiding a shared target/proc-macro artifact collision.
- `rch exec -- cargo test -p fp-join merge_outer_dense_int64_duplicates_matches_generic_validated_route --lib`: passed in isolated target.
- After rejection, `cargo fmt -p fp-join -- --check` passed and `git diff -- crates/fp-join/src/lib.rs` is empty.

## Next route

Do not repeat CSR-only periodic certification. The profile-backed next primitive is `br-frankenpandas-uza04.60`: a deeper proof-carrying dense outer periodic run-schedule descriptor that attacks plan construction and redundant plan walks, with target ratio `>=1.15x` on `outer_join 500000x200`.
