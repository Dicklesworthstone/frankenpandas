# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: br-frankenpandas-uza04.6 / crates/fp-io CSV read residual
# Total Passes: 5
# Started: 2026-06-04T13:18:00-04:00

## Status: COMPLETED - 5 passes applied; next pass must pivot primitives

## Missions
1. Baseline And Profile: Capture RCH-built CSV baseline, golden sha256, profile evidence, and a scored opportunity matrix.
2. Scanner Primitive: Apply one cache-local byte-scanner or DFA primitive only if the score is at least 2.0.
3. Proof Harness: Lock ordering, dtype, null/NaN, float-bit, and golden-output invariants with focused CSV tests.
4. Remote Rebench: Rebuild via RCH, verify golden sha256, run paired hyperfine, and keep only a real win.
5. Reprofile And Route: Reprofile after the accepted/rejected lever, close the bead, and select the next non-overlapping perf bead.

## Completed Passes
### Pass 1 - Baseline And Profile - 2026-06-04T13:21:00-04:00
- Files changed: none by subagent; local evidence artifacts captured separately.
- Changes: RCH-built baseline binary captured; combined CSV golden SHA `0c42e287275fb5440713769cf79e8bdd22e424d561f086ebe642d03661f174ec`; hyperfine baseline `csv_read` 494.3 ms +/- 11.7, `csv_read_options` 504.3 ms +/- 35.8, `csv_read_no_na_filter` confirmation 487.9 ms +/- 9.5.
- Recommendation: safe-Rust broadword delimiter scanner feeding the existing numeric parser/fallback logic; score 6.7.
- Verdict: PRODUCTIVE.

### Pass 2 - Scanner Primitive - 2026-06-04T15:31:00-04:00
- Files changed: `crates/fp-io/src/lib.rs` temporarily, then source hunk removed after benchmark rejection.
- Changes: Tried safe broadword/SWAR structural-byte detection for the simple numeric CSV fast path while preserving existing field parser and fallback behavior.
- Proof: combined CSV golden stayed byte-identical (`0c42e287275fb5440713769cf79e8bdd22e424d561f086ebe642d03661f174ec`).
- Benchmark: paired hyperfine showed `csv_read` 505.6 ms -> 481.1 ms (~1.05x), `csv_read_options` 501.3 ms -> 501.4 ms (flat), `csv_read_no_na_filter` 508.7 ms -> 536.9 ms (regressed/noisy).
- Verdict: REJECTED; Score < 2.0. Source hunk removed. Next pass must use a different primitive, not scanner-loop polish.

### Pass 3 - Persistent Worker-Pool Morsels - 2026-06-04T15:50:00-04:00
- Files changed: `Cargo.toml`, `Cargo.lock`, `crates/fp-io/Cargo.toml`, and `crates/fp-io/src/lib.rs` temporarily, then source/dependency hunk removed after benchmark rejection.
- Changes: Tried Rayon 1.12 scoped parallel morsels to replace per-read `std::thread::scope` worker spawning while preserving the existing chunk splitter, parser, merge order, and fallback behavior.
- Proof: combined CSV golden stayed byte-identical (`434a0ecec4b33d7d0ff1b232053fca83d4ca07b685f0351d13a083fdbfbf9333`).
- Benchmark: baseline `csv_read` 498.8 ms +/- 7.7, `csv_read_options` 496.3 ms +/- 7.2, `csv_read_no_na_filter` 483.7 ms +/- 12.0. Paired/focused rebench showed `csv_read` only 498.1 ms -> 486.0 ms (~1.02x), `csv_read_options` mixed/noisy, and `csv_read_no_na_filter` flat at ~489 ms. Strace before showed 80 `clone3` calls per 20 reads; candidate created a larger Rayon pool and heavy futex/yield traffic.
- Verdict: REJECTED; Score < 2.0. Source/dependency hunk removed. Next pass must use a different primitive, not scanner loops, header admission, token tables, or thread-pool swaps.

### Pass 4 - Exact Small-Decimal Parser - 2026-06-04T16:00:00-04:00
- Files changed: `crates/fp-io/src/lib.rs` temporarily, then source hunk removed after benchmark rejection.
- Changes: Tried a tightly gated safe ASCII parser for integer and one-decimal numeric fields with mantissa <= 1,000,000, falling back to `fast_float2` for long decimals/exponents/special values/overflow. Prototype accepted 725,772 of 1,000,000 generated fields with zero bit mismatches.
- Proof: combined CSV golden stayed byte-identical (`434a0ecec4b33d7d0ff1b232053fca83d4ca07b685f0351d13a083fdbfbf9333`).
- Benchmark: paired hyperfine regressed all scenarios: `csv_read` 486.9 ms -> 513.6 ms, `csv_read_options` 511.2 ms -> 531.9 ms, `csv_read_no_na_filter` 483.4 ms -> 521.7 ms.
- Verdict: REJECTED; Score < 2.0. Source hunk removed. Next pass must move to a different structural target, not CSV parser fast-path branches.

### Pass 5 - Shifted Int64 Range Direct Path - 2026-06-04T16:34:33-04:00
- Files changed: `crates/fp-frame/src/lib.rs` and `crates/fp-columnar/src/lib.rs` temporarily; candidate source hunk rejected and removed from the working tree. `HEAD` briefly contained the `fp-frame` half without the matching `fp-columnar` method, so the corrective source diff removes only that rejected range path.
- Changes: Tried a contiguous shifted-Int64-range Float64 arithmetic path for `series_add_align`, replacing sorted union position-vector materialization with direct output offsets for overlapping/touching unit-stride ranges.
- Proof: `series_add_align` golden stayed byte-identical, SHA `2b716d47c784429314fd64c9aa24ae9ca8fa4569e928c317fc8c2e1258c4b5c2`; focused `fp-frame` test `series_add_aligns_on_union_index` passed via RCH.
- Benchmark: initial baseline `series_add_align 100000 50` was 268.2 ms +/- 8.9; paired candidate rebench was before 249.9 ms +/- 7.3 vs candidate 271.6 ms +/- 8.9, a regression. Corrective removal binary kept the same golden SHA and ran 252.0 ms +/- 15.1 versus candidate 259.8 ms +/- 10.3.
- Verdict: REJECTED; Score < 2.0 for the direct range path. Do not keep iterating range position-map micro-tuning. Next attack should be a different primitive, specifically an AACE alignment-plan/result cache keyed by stable index label identities or another alien-graveyard structural cache/arena strategy with profile proof.
