# fuzz_groupby_agg_dispatch artifacts

New crashes from local or CI fuzz runs land here first.
Minimize them with cargo fuzz tmin fuzz_groupby_agg_dispatch <artifact> and then promote the minimized input into fuzz/corpus/fuzz_groupby_agg_dispatch/.
