# fuzz_series_add artifacts

New crashes from local or CI fuzz runs land here first.
Minimize them with cargo fuzz tmin fuzz_series_add <artifact> and then promote the minimized input into fuzz/corpus/fuzz_series_add/.
